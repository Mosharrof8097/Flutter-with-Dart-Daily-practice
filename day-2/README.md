# 🚀 ডার্ট কালেকশন মাস্টারক্লাস - ২য় দিন (Dart List Practice)

স্বাগতম! আমার ফ্লাটার এবং ডার্ট শেখার যাত্রার ২য় দিনে আজ আমি ডার্টের অন্যতম শক্তিশালী ডাটা স্ট্রাকচার **List** নিয়ে কাজ করেছি। আমি এমন কিছু প্রবলেম সলভ করেছি যা সরাসরি ফ্লাটার অ্যাপ ডেভেলপমেন্টে কাজে লাগে।

## 🛠️ যে সকল প্রবলেম সলভ করেছি:

### ১. স্টুডেন্ট লিস্ট ম্যানেজমেন্ট (Basic)
- **টাস্ক:** ৫ জন ছাত্রের নাম নিয়ে একটি লিস্ট তৈরি করা।
- **শিখলাম:** ডার্টে লিস্ট ডিক্লারেশন এবং ইনিশিয়ালাইজেশন।

### ২. ইনডেক্স ভিত্তিক ডাটা ইনসার্ট (Basic)
- **টাস্ক:** লিস্টের ১ নম্বর ইনডেক্সে একটি নতুন নাম যোগ করা।
- **শিখলাম:** `.insert()` মেথড ব্যবহার করে নির্দিষ্ট স্থানে ডাটা বসানো।

### ৩. স্ট্রিং ট্রান্সফরমেশন (Basic)
- **টাস্ক:** সব নামকে Uppercase (বড় হাতের অক্ষর) করে নতুন একটি লিস্ট বানানো।
- **শিখলাম:** `.map()` মেথড এবং `.toUpperCase()` এর ব্যবহার।

### ৪. ই-কমার্স কার্ট লজিক (Filtering)
- **টাস্ক:** শপিং কার্ট থেকে একটি নির্দিষ্ট আইটেম রিমুভ করা এবং কার্ট আপডেট চেক করা।
- **শিখলাম:** `.remove()`, `.contains()` এবং রিয়েল-লাইফ অ্যাপ লজিক।

### ৫. ইউজার রোল ম্যানেজমেন্ট (Syncing)
- **টাস্ক:** অ্যাপে নতুন রোল (Editor) আছে কি না চেক করা এবং না থাকলে তা যোগ করা।
- **শিখলাম:** কন্ডিশনাল ডাটা এডিং এবং লুপের মাধ্যমে ডাটা ফরম্যাটিং।

### ৬. সোশ্যাল মিডিয়া সার্চ লজিক (Advanced Filtering)
- **টাস্ক:** সার্চ বার ফাংশনালিটি ব্যবহার করে নির্দিষ্ট অক্ষর (যেমন: 'r') দিয়ে শুরু হওয়া নামগুলো খুঁজে বের করা।
- **শিখলাম:** `.where()` মেথড এবং অ্যারো ফাংশন (`=>`) এর পাওয়ারফুল ব্যবহার।

## 💡 আজকের প্রধান শিক্ষা (Key Learnings)
- **Arrow Functions (`=>`):** কোডকে এক লাইনে এবং ক্লিনভাবে লেখার উপায়।
- **Functional Methods:** `.where()` এবং `.map()` মেথড যা সি++ এর তুলনায় অনেক দ্রুত কাজ করে।
- **Logic Building:** অ্যাপের সার্চ বার বা ফিল্টারিং সিস্টেম কীভাবে ব্যাকএন্ডে কাজ করে তার ধারণা।

---
**পরবর্তী লক্ষ্য:** ডার্ট SET shikha  
