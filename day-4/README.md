# 🚀 Dart Map & Mixed Collection Mastery - Day 4

স্বাগতম! আমার ফ্লাটার এবং ডার্ট লার্নিং জার্নির ৪র্থ দিনে আজ আমি ডার্টের সবচেয়ে গুরুত্বপূর্ণ ডাটা স্ট্রাকচার **Map** এবং এর সাথে **List** ও **Set** এর সমন্বয়ে জটিল অ্যাপ লজিক নিয়ে কাজ করেছি। ২০২৫ সালের আধুনিক ফ্লাটার ডেভেলপমেন্টে ডাটা হ্যান্ডেল করার জন্য এই কনসেপ্টগুলো অপরিহার্য।

## 🛠️ সলভ করা চ্যালেঞ্জসমূহ:

### ১. ইউজার প্রোফাইল ও ডাইনামিক আপডেট (Pure Map)
- **লজিক:** একটি ইউজার প্রোফাইল ম্যাপ তৈরি করে তাতে নতুন ডাটা যোগ করা, মান পরিবর্তন করা এবং কন্ডিশনাল চেকিং।
- **শিখলাম:** `.containsKey()`, `forEach()` এবং ম্যাপের ভ্যালু আপডেট করার পদ্ধতি।

### ২. স্টুডেন্ট মার্কস ফিল্টারিং (Data Cleaning)
- **লজিক:** একটি ম্যাপ থেকে নির্দিষ্ট কন্ডিশনের ওপর ভিত্তি করে ডাটা রিমুভ করা (যারা ফেল করেছে তাদের বাদ দেওয়া)।
- **শিখলাম:** `.removeWhere()` মেথডের প্রফেশনাল ব্যবহার।

### ৩. ম্যাপ টু লিস্ট কনভার্সন (Calculation)
- **লজিক:** ম্যাপের ভ্যালুগুলোকে আলাদা করে লিস্টে নেওয়া এবং গাণিতিক হিসাব করা।
- **শিখলাম:** `.values.toList()` এবং লুপের মাধ্যমে যোগফল বের করা।

### 🔵 ৪. সোশ্যাল মিডিয়া অ্যানালিটিক্স (Master Challenge - Mixed)
- **লজিক:** একটি `List<Map>` থেকে সব ট্যাগগুলো সংগ্রহ করে একটি **Set** এ রাখা যাতে কোনো ডুপ্লিকেট ট্যাগ না থাকে। এটি একটি রিয়েল-ওয়ার্ল্ড সোশ্যাল মিডিয়া অ্যাপের ট্যাগ ক্লাউড লজিক।
- **শিখলাম:** `List`, `Map` এবং `Set` এর সমন্বিত ব্যবহার এবং নেস্টেড ডাটা স্ট্রাকচার হ্যান্ডলিং।

## 💡 আজকের প্রধান শিক্ষা (Key Learnings):
- **Key-Value Pair:** ডার্টে কীভাবে অবজেক্ট স্টাইলে ডাটা অর্গানাইজ করতে হয়।
- **Null Safety in Maps:** ম্যাপের ভ্যালু এক্সেস করার সময় `!` এবং `??` অপারেটরের প্রয়োজনীয়তা।
- **Nested Collections:** লিস্টের ভেতরে ম্যাপ এবং ম্যাপের ভেতরে লিস্ট থাকলে কীভাবে লুপ চালাতে হয়।

**Single value → map((e) => e['key'] as Type).toList()**
**List inside Map → expand((e) => e['key'] as List<Type>).toList()**

---
**পরবর্তী লক্ষ্য:** ডার্ট ফাংশন (Advanced) এবং অবজেক্ট ওরিয়েন্টেড প্রোগ্রামিং (OOP) এর সূচনা।
