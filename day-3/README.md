# 🚀 Dart Set Mastery - Day 3 (Uniqueness & Logic)

স্বাগতম! আমার ফ্লাটার এবং ডার্ট লার্নিং জার্নির ৩য় দিনে আজ আমি ডার্টের **Set** কালেকশন নিয়ে কাজ করেছি। অ্যাপ ডেভেলপমেন্টে ডাটার ইউনিকনেস নিশ্চিত করা এবং দ্রুত সার্চিং করার জন্য Set একটি অপরিহার্য টুল।

## 🛠️ সলভ করা রিয়েল-ওয়ার্ল্ড প্রবলেমস:

### ১. ডুপ্লিকেট কন্টাক্ট রিমুভার (Contact Cleaning)
- **লজিক:** একটি লিস্টে থাকা ডুপ্লিকেট নামগুলো সরিয়ে শুধু ইউনিক কন্টাক্টগুলো রাখা।
- **শিখলাম:** `.toSet().toList()` মেথড ব্যবহার করে ডাটা ক্লিন করা।

### ২. ইউজার ইন্টারেস্ট ট্র্যাকিং (Ecommerce Logic)
- **লজিক:** শপিং অ্যাপে ইউজার কোন কোন ক্যাটাগরি ব্রাউজ করছে তা ট্র্যাক করা এবং ডুপ্লিকেট এন্ট্রি রোধ করা।
- **শিখলাম:** `.add()` এবং `.addAll()` এর মাধ্যমে ইউনিক ডাটা ইনসার্ট করা।

### ৩. মিউচুয়াল ফ্রেন্ডস ফাইন্ডার (Intersection)
- **লজিক:** দুজন ইউজারের ফ্রেন্ড লিস্টের মধ্যে কমন ফ্রেন্ডদের খুঁজে বের করা।
- **শিখলাম:** `.intersection()` মেথড ব্যবহার করে দুই সেটের কমন এলিমেন্ট বের করা।

### ৪. প্রিমিয়াম ফিচার এক্সেস কন্ট্রোল (Security Check)
- **লজিক:** ইউজারের কাছে নির্দিষ্ট ফিচারের এক্সেস আছে কি না তা দ্রুত ভেরিফাই করা।
- **শিখলাম:** `.contains()` মেথড যা লিস্টের তুলনায় অনেক বেশি ফাস্ট।

### ৫. ইউনিক ট্যাগ সিস্টেম (Blog App)
- **লজিক:** ব্লগ পোস্টের জন্য রাইটার ভুল করে একই ট্যাগ বারবার দিলেও তা একবারই সেভ করা।
- **শিখলাম:** সেটের মাধ্যমে অটোমেটিক ডুপ্লিকেট হ্যান্ডলিং।

### ৬. একটিভ ইউজার ফিল্টারিং (Difference)
- **লজিক:** টোটাল ইউজার থেকে ব্লকড ইউজারদের সরিয়ে শুধুমাত্র একটিভ ইউজারদের বের করা।
- **শিখলাম:** `.difference()` মেথড ব্যবহার করে এক সেট থেকে অন্য সেটের বিয়োগফল বের করা।

## 💡 আজকের প্রধান শিক্ষা (Key Takeaways):
- **Uniqueness:** সেট কখনো ডুপ্লিকেট ভ্যালু গ্রহণ করে না।
- **Efficiency:** বড় ডাটা সেটে কোনো আইটেম সার্চ করার জন্য সেট সবথেকে কার্যকর।
- **Set Operations:** `intersection`, `difference` এবং `union` এর মাধ্যমে গাণিতিক সেট অপারেশনগুলো অ্যাপ লজিকে ইমপ্লিমেন্ট করা।

---
**পরবর্তী লক্ষ্য:** ডার্ট ম্যাপ (Map) - JSON ডাটা হ্যান্ডলিং এবং Null Safety প্র্যাকটিস।
